# Portfólio (Nuxt Content)

Conteúdo editável no **Nuxt Studio** (`/_studio`).

- `pt/` — português  
- `en/` — inglês  

Altere JSON aqui ou use `npm run sync:content` para regenerar a partir de `locales/`.
